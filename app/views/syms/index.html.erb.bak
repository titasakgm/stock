<h1>Listing syms</h1>

<%= paginate @syms %>
<p>
<table>
  <tr>
    <th>Name</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @syms.each do |sym| %>
  <tr>
    <td><%= link_to sym.name, "/quotes/search/#{sym.name}" %></td>
    <td><%= link_to 'Show', sym %></td>
    <td><%= link_to 'Edit', edit_sym_path(sym) %></td>
    <td><%= link_to 'Destroy', sym, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Sym', new_sym_path %>
